{
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/node",
            "config": {
                "buildCommand": "bun run dev",
                "outputDirectory": "dist"
            }
        }
    ]
}